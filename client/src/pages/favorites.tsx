import { LayoutWithNavbar } from '@/components/Layouts';

const Favorites = () => {
  return (
    <LayoutWithNavbar>
      THIS IS THE FAVORITES PAGE. HERE YOU CAN SEE ALL YOUR FAVORITES LISTINGS.
      THESE WOULD BE FETCHED FROM THE API
    </LayoutWithNavbar>
  );
};

export default Favorites;
